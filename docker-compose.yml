services:
  matplotlib:
    build: .
    container_name: matplotlib_gui
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - .:/app
    working_dir: /app
    network_mode: "host"
    stdin_open: true
    tty: true
